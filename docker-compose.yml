version: '3.8'

services:
  db:
    image: postgres:15
    container_name: springBootCrud
    restart: always
    environment:
      POSTGRES_DB: springBootCrud
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"